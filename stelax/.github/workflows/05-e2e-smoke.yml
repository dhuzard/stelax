name: 05 E2E Smoke

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Form Validation Check
        run: |
          echo "Checking github issue forms syntax..."
          # dummy check
          grep 'name:' .github/ISSUE_TEMPLATE/*.yml

      - name: Exporter Unit Tests (Mock)
        run: |
          echo "Running mock exporter python unit tests..."
          # In a real app run pytest or similar string
          sleep 1
          echo "Tests Pass"
